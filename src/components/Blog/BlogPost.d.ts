import type { JSX } from 'react';
import type { BlogPostProps } from '../../types';
export declare const BlogPost: ({ id, image, lengthComments, date, title, content, }: BlogPostProps) => JSX.Element;
